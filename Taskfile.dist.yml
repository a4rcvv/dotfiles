version: "3"

tasks:
  docker:build:
    desc: "Build a container"
    cmds:
      - docker compose build
  docker:run:
    desc: "Run Ubuntu container"
    cmds:
      - docker compose run --rm ubuntu